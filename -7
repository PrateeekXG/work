I have a CSV containing:
incident_id
short_description
description
work_notes

Generate a Python script that:
1. Reads the CSV
2. Combines the text fields
3. Creates embeddings using BAAI/bge-small-en-v1.5
4. Stores them in a Qdrant collection named incidents






pip install qdrant-client
pip install sentence-transformers
pip install pandas


python ingest.py



Create a Python function search_similar_incidents(query, top_k=5)
that:
1. Creates an embedding using bge-small-en-v1.5
2. Searches Qdrant collection incidents
3. Returns incident_id, score, short_description and work_notes


search.py




results = search_similar_incidents(
    "rating worksheet comparator failing"
)




pip install mcp

Create an MCP server using FastMCP.
Expose a tool called search_similar_incidents.
The tool should call the search function from search.py and return top 5 incidents.
save as- server.py

python server.py




pip install streamlit

Create a Streamlit app with:

- Title: Guidewire Incident Copilot
- Text area for incident description
- Search button
- Display top 5 similar incidents
- Show similarity score
- Expandable work notes section
save as- app.py


###########

I already have a semantic search application using Qdrant vector database and Streamlit.

Create a new Python file called test_ollama.py that connects to a locally running Ollama instance and sends a simple prompt to model qwen3:4b.

Print the response to console.

Use the latest Ollama Python library and provide complete code and required pip install commands.







I have a function search_similar_incidents(query, top_k) that returns the most similar incidents from Qdrant.

Create a new function generate_incident_analysis(query, incidents).

The function should:

1. Accept the user incident description.
2. Accept the retrieved incidents.
3. Build a prompt containing the incident description and historical incidents.
4. Call Ollama model qwen3:4b.
5. Return:
   - Probable root cause
   - Supporting evidence
   - Recommended resolution
   - Confidence level

Provide complete Python implementation.






Refactor my application into a Retrieval Augmented Generation (RAG) workflow.

Current flow:
User Query -> Qdrant Search -> Similar Incidents

Target flow:
User Query -> Qdrant Search -> Retrieve Top 5 Incidents -> Build Context -> Ollama (qwen3:4b) -> Generate RCA and Resolution

Show architecture and code changes required in search.py.







Update my Streamlit application so that after similar incidents are retrieved, the user can click "Generate RCA". The button should call the Ollama-powered RAG workflow and display:
Root Cause Analysis
Recommended Resolution
Confidence Score
Historical Incidents Used










